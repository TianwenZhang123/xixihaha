# VMAD 实验记录

> **环境**：AutoDL A800 80GB PCIe，Wan2.1-T2V-1.3B-Diffusers，30 steps，guidance=5.0，seed=42，480×832，81帧
> **评测指标**：`orig_gen_clip`（帧级外观相似度）、`orig_gen_xclip`（时序语义相似度）
> **样本**：10 个（ID: 7, 17, 21, 31, 32, 33, 34, 43, 46, 47）

---

## 一、P-Flow Baseline 参考值

| 配置 | CLIP | XCLIP | 说明 |
|------|------|-------|------|
| VLM baseline caption（10样本均值） | 0.8703 | 0.7164 | P-Flow 起点 |
| V4 LLM 改写 iter1（10样本均值） | 0.8842 | 0.7430 | P-Flow Layer 1 当前最优 |
| VLM baseline（sample #7） | 0.9159 | 0.7108 | VMAD 实验的锚点 |

P-Flow V4 改写三原则：Subject-First Opening + Temporal Action Chain + Preserve Visual Vocabulary。V4 caption 来源：`/root/autodl-tmp/outputs/hybrid_iter_v4/captions_iter0/`

---

## 二、Bug 修复与 Pipeline 等价性验证

**时间**：2025-05-31

### 发现的 Bug

| # | Bug | 位置 | 影响 | 修复 |
|---|-----|------|------|------|
| 1 | `apply_noise_prior` 无条件调用 | pipeline.py L657 | generator 状态被额外 `torch.randn` 消耗，后续 diffusion 随机序列偏移 | 加 `if cfg.use_blend:` |
| 2 | `motion_text` 无条件拼接 | pipeline.py L630 | 即使 `--no-token_decode`，仍拼接垃圾 token 到 prompt | 加 `and cfg.use_token_decode` |

**Token Decode 产生乱码的根因**：Token Decoder 在 T5 **输入 embedding 空间**做最近邻搜索，但 Δe 存在于 T5 **输出 hidden states 空间**（经过 12 层 Transformer）。两个空间几何结构完全不同。代码有 `_build_output_codebook()` 可在正确空间搜索但未被调用。

### 等价性验证（sample #7）

| 实验配置 | orig_gen_clip | orig_gen_xclip |
|----------|--------------|----------------|
| P-Flow baseline | 0.9159 | 0.7108 |
| P-Flow reseed（确认可复现） | 0.9159 | 0.7108 |
| VMAD 修复前（Bug 1+2） | 0.8007 | — |
| VMAD 修复 Bug1，Bug2 残留 | 0.8986 | 0.6384 |
| **VMAD 三层全关（两 bug 修复）** | **0.9150** | **0.7077** |

**结论**：修复后 VMAD pipeline 在三层全关时与 P-Flow 等价（CLIP 差异 < 0.001）。

---

## 三、Layer 2 (Δe) 效果验证

### 3.1 Baseline Caption + Strength 扫描（sample #7）

配置：关闭 L1(motion_text) + L3(blend)，仅保留 L2(velocity)。Asset ‖Δe‖=36.25。

| Alpha | orig_gen_clip | orig_gen_xclip | Δ CLIP vs 基线 |
|-------|--------------|----------------|----------------|
| 0（基线） | 0.9150 | 0.7077 | — |
| 0.001 | 0.9086 | 0.6857 | -0.0064 |
| **0.01** | **0.9192** | **0.6987** | **+0.0042** |
| 0.05 | 0.8999 | 0.5925 | -0.0151 |
| 0.1 | 0.9016 | 0.5670 | -0.0134 |

**分析**：倒 U 形曲线，α=0.01 时 CLIP 超过 P-Flow baseline。Δe 确实编码了有用运动信息。

### 3.2 V4 Caption × Layer 2 消融（sample #7）

2×2 矩阵验证 caption 质量与 Δe 注入的叠加关系：

| 编号 | Caption | 层配置 | orig_gen_clip | orig_gen_xclip |
|------|---------|--------|--------------|----------------|
| A | Baseline | 三层全关 | 0.9150 | 0.7077 |
| B | Baseline | L2 α=0.01 | 0.9192 | 0.6987 |
| **C** | **V4** | **三层全关** | **0.9397** | **0.7260** |
| **D** | **V4** | **L2 α=0.01** | **0.9394** | **0.7486** |

**核心结论**：

1. V4 caption 主攻 CLIP（+0.0247），Δe 主攻 XCLIP（在 V4 基础上 +0.0226）
2. 两者互补叠加但贡献维度不同——CLIP 被 V4 caption 饱和后，Δe 的残差修正被"释放"到时序维度
3. D 配置的 XCLIP=0.7486 超过 P-Flow 所有实验

### 3.3 Alpha 精扫（V4 Caption + L2 Only, sample #7）

| Alpha | orig_gen_clip | orig_gen_xclip | Δ CLIP vs C | Δ XCLIP vs C |
|-------|--------------|----------------|-------------|--------------|
| 0 (C) | 0.9397 | 0.7260 | — | — |
| **0.005** | **0.9446 ★** | **0.7541** | **+0.0049** | **+0.0281** |
| **0.008** | **0.9395** | **0.7581 ★** | **-0.0002** | **+0.0321** |
| 0.01 (D) | 0.9394 | 0.7486 | -0.0003 | +0.0226 |
| 0.015 | 0.9343 | 0.7519 | -0.0054 | +0.0259 |
| 0.02 | 0.9381 | 0.7299 | -0.0016 | +0.0039 |

**最优配置**：
- **CLIP 最优**：V4 caption + L2 α=0.005 → CLIP=**0.9446**, XCLIP=0.7541
- **XCLIP 最优**：V4 caption + L2 α=0.008 → CLIP=0.9395, XCLIP=**0.7581**

V4 caption 下最优 alpha 比 baseline 左移（0.005~0.008 vs 0.01），因为 V4 已将 e₀ 推到更接近目标的方向，Δe 只需更小修正。

---

## 四、Layer 3 (Noise Prior) 消融 — 结论：有害

**时间**：2025-06-01

| 编号 | Caption | 层配置 | orig_gen_clip | orig_gen_xclip |
|------|---------|--------|--------------|----------------|
| A | Baseline | 三层全关 | 0.9150 | 0.7077 |
| E | Baseline | L3 only (α=0.001) | 0.8097 | 0.6407 |
| F | Baseline | L2+L3 (α=0.01) | 0.7952 | 0.7025 |
| C | V4 | 三层全关 | 0.9397 | 0.7260 |
| G | V4 | L3 only (α=0.001) | 0.7993 | 0.6782 |
| H | V4 | L2+L3 (α=0.01) | 0.7875 | 0.6952 |

**结论：Layer 3 在当前实现中有害（CLIP 暴跌 -0.10~-0.15）。**

**根因分析**：`apply_noise_prior()` 调用 `torch.randn(generator=generator)` 消耗了共享 generator 的状态。但更深层问题是 **latents 生成路径差异** —— 外部用 `torch.randn` 生成的噪声与 pipeline 内部 `prepare_latents` 用 `randn_tensor` 生成的噪声，即使相同 generator 状态也会产生不同结果（因为 `randn_tensor` 的实现可能涉及分块生成或不同的内存布局）。

**重要修正**：此前"generator state offset"理论**部分不正确**。`FlowMatchEulerDiscreteScheduler` 是确定性调度器（`sigma=0`），在去噪循环中**不消耗 generator**。真正的问题是：
1. pipeline 的 `prepare_latents` 内部用 `randn_tensor` 生成初始噪声
2. 外部 `apply_noise_prior` 用 `torch.randn` 生成混合后的噪声
3. 即使 generator 状态相同，这两条路径产生的随机数**完全不同**（不只是偏移，是完全不同的数）

**修复方案（未实施）**：不再外部生成噪声，而是让 pipeline 正常生成初始噪声（通过 `prepare_latents`），然后在外部将 eta_motion blend 进去：
```python
# 让 pipeline 正常生成 latents（保证路径一致）
latents = pipe.prepare_latents(...)  # 内部用 randn_tensor
# 外部 blend eta_motion
latents = sqrt(alpha) * eta_motion + sqrt(1-alpha) * latents
```

**决策：当前阶段放弃 Layer 3，专注 Layer 2 + V4 caption。Layer 3 需要工程重构才能正确工作。**

---

## 五、当前全局最优与 P-Flow 对比

| 配置 | CLIP | XCLIP | 来源 |
|------|------|-------|------|
| P-Flow VLM baseline（10样本） | 0.8703 | 0.7164 | P-Flow |
| P-Flow V4 iter1（10样本） | 0.8842 | 0.7430 | P-Flow |
| **VMAD V4+L2 α=0.005（sample #7）** | **0.9446** | **0.7541** | VMAD ★CLIP最优 |
| **VMAD V4+L2 α=0.008（sample #7）** | **0.9395** | **0.7581** | VMAD ★XCLIP最优 |

注：VMAD 结果目前仅为单样本（#7），需多样本验证确认稳定性。

---

## 六、技术架构备忘

### VMAD Apply 流程（修复后）

```
content_prompt
     │
     ▼
[Layer 1] if use_token_decode: prompt += motion_text  ← 当前产生乱码，建议关闭
     │
     ▼
[Layer 2] if use_velocity: hook(encoder_output += effective_strength × Δe)
     │                     effective_strength = strength × alpha
     ▼
[Layer 3] if use_blend: latents blend  ⚠️ 有害！路径差异导致结果偏移！不应使用！
     │
     ▼
[Generate] pipe(prompt=full_prompt, latents=latents, generator=generator)
```

### cfg.alpha 的双重耦合

同一个 `cfg.alpha` 参数同时控制：
- Layer 2：`effective_strength = strength × alpha`（Δe 注入强度）
- Layer 3：`alpha_scaled = alpha × strength`（noise blending 比例）

这是设计问题——两层应该有独立参数。当前因 Layer 3 关闭所以不影响实验，但后续若修复 Layer 3 需要解耦。

### P-Flow vs VMAD Seed 策略差异

- P-Flow：`seed = cfg.seed + sample_id`（每个样本不同 seed）
- VMAD：`seed = cfg.seed`（固定 seed=42）

当前实验因均为 SELF 复现模式（同一 sample 对比自己），seed 策略差异不影响结果。但多样本评测时需注意一致性。

### 关键数值

- Asset ‖Δe‖ (sample 7, baseline caption): 36.25
- Asset ‖Δe‖ (sample 7, V4 caption): 36.75
- Asset ‖η_motion‖ (sample 7): 4192.0
- P-Flow baseline CLIP (sample 7): 0.9159
- VMAD 纯净基线 CLIP (sample 7): 0.9150

---

## 七、实验输出目录（完整索引）

### `/root/autodl-tmp/outputs/` — 主输出目录

| 目录 | 时间 | 内容 | 样本 | 状态 | 对应文档章节 |
|------|------|------|------|------|------------|
| `baseline/` | 05-27 | P-Flow 最初 baseline 实验 | 10 | ✅ 已用 | 一 |
| `baseline_captions/` | 05-30 | 汇总的 VLM baseline captions 文本文件 | 10 | 辅助文件 | — |
| `full/` | 05-27 | P-Flow 全功能实验（早期） | — | ⚠️ 已过时 | — |
| `hybrid_iter/` | 05-28 | P-Flow 迭代改写（V1~V3 版本） | 10 | ⚠️ 已被 V4 取代 | — |
| `hybrid_iter_v4/` | 05-28 | **P-Flow V4 迭代改写** | 10 | ✅ **核心资产** | 一 |
| `hybrid_prompt/` | 05-27 | P-Flow 结构化 prompt 实验 | 10 | ⚠️ 已过时 | — |
| `hybrid_svd/` | 05-28 | P-Flow + SVD noise prior 实验 | 10 | ❌ Layer3有害 | — |
| `inversion/` | 05-26 | Flow Matching Inversion 初始测试 | 1 | ⚠️ 早期调试 | — |
| `inversion_svd/` | 05-26 | Inversion + SVD 初始测试 | 1 | ⚠️ 早期调试 | — |
| `noise_prior/` | 05-27 | Noise Prior 单独实验 | — | ❌ Layer3有害 | — |
| `noise_prior_iter/` | 05-27 | Noise Prior 迭代实验 | — | ❌ Layer3有害 | — |
| `position_optimized/` | 05-27 | Position-Aware 优化实验 | 10 | ⚠️ 早期 | — |
| `structured_prompt/` | 05-27 | 结构化 prompt 实验 | 10 | ⚠️ 已被 V4 取代 | — |
| `ablation/` | 05-27 | 消融实验 | — | ⚠️ 早期 | — |
| `logs/` | 06-01 | 实验日志汇总 | — | 辅助文件 | — |
| — | — | — | — | — | — |
| **VMAD Pipeline 验证阶段** | | | | | |
| `vmad_phase1_test/` | 05-30 | VMAD 初始 pipeline 测试 | 1 | ⚠️ 调试用 | 二 |
| `vmad_phase1_test2/` | 05-30 | Pipeline 测试 2 | 1 | ⚠️ 调试用 | 二 |
| `vmad_phase1_test3/` | 05-30 | Pipeline 测试 3 | 1 | ⚠️ 调试用 | 二 |
| `vmad_phase1_test4/` | 05-30 | Pipeline 测试 4 | 1 | ⚠️ 调试用 | 二 |
| `vmad_minimal_v2/` | 05-31 | VMAD 最小化验证 | 1 | ⚠️ 调试用 | 二 |
| `vmad_noblend_fixed/` | 05-31 | Bug1 修复验证（无 blend） | 1 | ✅ 已用 | 二 |
| `pflow_reseed/` | 05-31 | P-Flow reseed 等价性确认 | 1 | ✅ 已用 | 二 |
| `prompt_embeds_diagnostic/` | 05-31 | Prompt embedding 诊断分析 | 1 | 辅助文件 | — |
| — | — | — | — | — | — |
| **Layer 2 单样本验证** | | | | | |
| `vmad_pure_baseline/` | 05-31 | 三层全关纯净基线 (A) | 1(#7) | ✅ 已用 | 三.1 |
| `vmad_layer2_only/` | 05-31 | L2 only 初始测试 | 1(#7) | ⚠️ 早期 | — |
| `vmad_l2_a001/` | 05-31 | Baseline + L2 α=0.001 | 1(#7) | ✅ 已用 | 三.1 |
| `vmad_l2_a005/` | 05-31 | Baseline + L2 α=0.05 | 1(#7) | ✅ 已用 | 三.1 |
| `vmad_l2_a010/` | 05-31 | Baseline + L2 α=0.1 | 1(#7) | ✅ 已用 | 三.1 |
| `vmad_v4caption_pure/` | 05-31 | V4 + 三层全关 (C) | 1(#7) | ✅ 已用 | 三.2 |
| `vmad_v4caption_l2/` | 05-31 | V4 + L2 α=0.01 (D) | 1(#7) | ✅ 已用 | 三.2 |
| — | — | — | — | — | — |
| **Layer 2 多样本验证** | | | | | |
| `vmad_phase3a_full/` | 05-31 | **10 样本完整 extract（核心 assets）** | 10 | ✅ **核心资产** | — |
| `vmad_v4_10samples/` | 06-01 | **V4 caption 10样本 extract（含 assets/delta_e）** | 10 | ✅ **核心资产** | — |
| `vmad_v4_10samples_a005_pss` | 06-01 | V4+L2 α=0.005, per-sample-seed | 10 | ❌ **有 bug** | — |
| `vmad_v4_10samples_a008/` | 06-01 | V4+L2 α=0.008（旧代码） | 10 | ❌ **有 bug** | — |
| `vmad_v4_10samples_a008_pss` | 06-01 | V4+L2 α=0.008, per-sample-seed | 10 | ❌ **有 bug** | — |
| `vmad_v4_10samples_ctrl/` | 06-01 | V4 ctrl（旧代码） | 10 | ❌ **有 bug** | — |
| `vmad_v4_10samples_ctrl_pss` | 06-01 | V4 ctrl, per-sample-seed | 10 | ❌ **有 bug** | — |
| — | — | — | — | — | — |
| **Bug 修复验证（norm-adaptive）** | | | | | |
| `fix_validation/` | 06-02 | norm-adaptive bug 定位实验 | 1(#7) | ✅ 确认了 bug | — |
| `quick_validate/` | 06-02 | **修复后快速验证（seed=42）** | 1(#7) | ✅ **修复确认** | — |
| `l2_direct_validation/` | 06-02 | **🔄 正在跑：修复后 10 样本全量验证** | 10 | 🔄 进行中 | — |

### `/root/autodl-tmp/outputs111/` — 单样本精扫（05-31，sample #7）

| 目录 | 内容 | 结果 | 对应文档章节 |
|------|------|------|------------|
| `vmad_l3_only/` | E) Baseline + L3 only α=0.001 | CLIP=0.8097 ❌ | 四 |
| `vmad_l2l3/` | F) Baseline + L2+L3 α=0.01 | CLIP=0.7952 ❌ | 四 |
| `vmad_v4caption_l3/` | G) V4 + L3 only α=0.001 | CLIP=0.7993 ❌ | 四 |
| `vmad_v4caption_l2l3/` | H) V4 + L2+L3 α=0.01 | CLIP=0.7875 ❌ | 四 |
| `vmad_v4_alpha_0.005/` | **I) V4+L2 α=0.005 ★CLIP最优** | **CLIP=0.9446** ✅ | 三.3 |
| `vmad_v4_alpha_0.008/` | **J) V4+L2 α=0.008 ★XCLIP最优** | **XCLIP=0.7581** ✅ | 三.3 |
| `vmad_v4_alpha_0.015/` | K) V4+L2 α=0.015 | CLIP=0.9343 | 三.3 |
| `vmad_v4_alpha_0.02/` | L) V4+L2 α=0.02 | CLIP=0.9381 | 三.3 |

### `/root/autodl-tmp/` — 其他文件

| 路径 | 说明 |
|------|------|
| `data/video-200/water_mark_out/` | **200 个参考视频（核心数据）** |
| `models/` | Wan2.1、CLIP、X-CLIP 等模型权重 |
| `videofake/` | 代码仓库（git） |
| `experiment_results/` | 早期实验结果备份 |
| `pure_position_results/` | position bias 分析结果 |
| `dit_position_experiment.py` | 早期 position bias 实验脚本 |
| `position_scan.py` | position 扫描脚本 |
| `pure_position_experiment.py` | 纯 position 实验脚本 |
| `23.tar.gz` | 打包备份 |
| `yu.tar.gz` | 打包备份 |

### 状态说明

- ✅ **核心资产** — 当前实验依赖，不可删除
- ✅ 已用 — 数据已记录到文档，保留备查
- ❌ **有 bug** — norm-adaptive scaling bug 导致结果无效，用修复后的 `l2_direct_validation` 替代
- ❌ Layer3有害 — 已证实无效
- ⚠️ 已过时/早期 — 被后续实验取代，可考虑清理
- 🔄 进行中 — 当前正在运行

### 可安全清理的目录（释放空间）

```bash
# 早期调试（已有更好结果）
rm -rf /root/autodl-tmp/outputs/vmad_phase1_test{,2,3,4}
rm -rf /root/autodl-tmp/outputs/vmad_minimal_v2
rm -rf /root/autodl-tmp/outputs/vmad_layer2_only
rm -rf /root/autodl-tmp/outputs/inversion{,_svd}

# 已被 V4 取代的 P-Flow 旧版实验
rm -rf /root/autodl-tmp/outputs/hybrid_prompt
rm -rf /root/autodl-tmp/outputs/structured_prompt
rm -rf /root/autodl-tmp/outputs/position_optimized
rm -rf /root/autodl-tmp/outputs/full

# Layer 3 有害实验（数据已记录）
rm -rf /root/autodl-tmp/outputs/noise_prior{,_iter}
rm -rf /root/autodl-tmp/outputs/hybrid_svd

# norm-adaptive bug 导致的无效结果
rm -rf /root/autodl-tmp/outputs/vmad_v4_10samples_a005_pss
rm -rf /root/autodl-tmp/outputs/vmad_v4_10samples_a008_pss
rm -rf /root/autodl-tmp/outputs/vmad_v4_10samples_ctrl_pss
rm -rf /root/autodl-tmp/outputs/vmad_v4_10samples_a008
rm -rf /root/autodl-tmp/outputs/vmad_v4_10samples_ctrl
rm -rf /root/autodl-tmp/outputs/fix_validation

# AutoDL 根目录早期脚本
rm -f /root/autodl-tmp/dit_position_experiment.py
rm -f /root/autodl-tmp/position_scan.py
rm -f /root/autodl-tmp/pure_position_experiment.py
rm -rf /root/autodl-tmp/pure_position_results
rm -rf /root/autodl-tmp/experiment_results
```
