# P-Flow Video Reproduction Requirements
# Target: AutoDL A800 (80GB) — Wan2.1-1.3B + Local Qwen2.5-VL-7B

# Config parsing (Python ≥ 3.11 内置 tomllib, 否则用此备用)
tomli>=1.2.0

# Core dependencies
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Video generation model (Wan 2.1-1.3B)
diffusers>=0.31.0
transformers>=4.45.0
accelerate>=0.30.0
safetensors>=0.4.0

# Local VLM (Qwen2.5-VL-7B)
qwen-vl-utils>=0.0.8           # Qwen-VL image/video preprocessing
# bitsandbytes>=0.43.0          # Uncomment for 4-bit/8-bit quantization

# VLM client (DashScope fallback, optional)
openai>=1.12.0

# Video processing
opencv-python>=4.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
eva-decord>=0.6.0               # Fast video decoding
av>=12.0.0                      # PyAV for video I/O

# ── Evaluation metrics (6 项) ──
scipy>=1.11.0                   # FVD 矩阵平方根
scikit-learn>=1.3.0
scikit-image>=0.22.0            # SSIM
lpips>=0.1.4                    # LPIPS 感知距离

# Numeric & utilities
numpy>=1.24.0
pandas>=2.0.0
Pillow>=10.0.0
tqdm>=4.66.0
pyyaml>=6.0
einops>=0.7.0

# Shot Boundary Detection
transnetv2-pytorch>=0.1.0       # TransNetV2 (recommended SBD backend)
scenedetect[opencv]>=0.6.0      # PySceneDetect (lightweight fallback)

# Data management
huggingface-hub>=0.20.0         # Model/dataset downloading
