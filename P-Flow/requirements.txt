# P-Flow Video Reproduction Requirements
# Target: AutoDL A800 (80GB) — Wan2.1-1.3B + Local Qwen2.5-VL-7B

# Config parsing (Python ≥ 3.11 内置 tomllib, 否则用此备用)
tomli>=1.2.0

# Core dependencies
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Video generation model (Wan 2.1-1.3B)
diffusers>=0.31.0
transformers>=4.45.0
accelerate>=0.30.0
safetensors>=0.4.0

# Local VLM (Qwen2.5-VL-7B, optional for auto-captioning)
qwen-vl-utils>=0.0.8

# VLM client (DashScope fallback, optional)
openai>=1.12.0

# Video processing
opencv-python>=4.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
eva-decord>=0.6.0
av>=12.0.0

# Numeric & utilities
numpy>=1.24.0
pandas>=2.0.0
Pillow>=10.0.0
tqdm>=4.66.0
pyyaml>=6.0
einops>=0.7.0

# Shot Boundary Detection (optional)
transnetv2-pytorch>=0.1.0
scenedetect[opencv]>=0.6.0

# Data management
huggingface-hub>=0.20.0

# ── 以下为评测所需 ──
# CLIP / XCLIP / DINO 评测 (已由 transformers 覆盖)
# LPIPS 感知距离
lpips>=0.1.4
# SSIM 结构相似度
scikit-image>=0.22.0
# FVD 矩阵平方根
scipy>=1.11.0
# CLIP 相似度 & FVD
scikit-learn>=1.3.0
