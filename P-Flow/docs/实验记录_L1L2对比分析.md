# P-Flow L1 & L2 对比实验记录

> **实验时间**：2025-06-09 ~ 2025-06-10
>
> **实验环境**：AutoDL A800 80GB，服务器路径 `/root/xixihaha/P-Flow`
>
> **生成模型**：Wan2.1-T2V-1.3B（UMT5 text encoder）
>
> **生成参数**：steps=30, guidance=5.0, seed=42
>
> **样本集**：10 个固定样本（IDs: 7, 17, 21, 31, 32, 33, 34, 43, 46, 47）
>
> **评测指标**：`orig_gen_clip` = 原始视频与生成视频的 CLIP 帧级相似度；`orig_gen_xclip` = 原始视频与生成视频的 X-CLIP 时序语义相似度
>
> **Caption 来源**：`/root/xixihaha/test-v200/test-v200/captions`（VLM 原始 caption）
>
> **参考视频**：`/root/xixihaha/P-Flow/data/reference_videos/`

---

## 一、方法概述

### 1.1 P-Flow 三层架构

P-Flow 采用分层条件注入框架（Hierarchical Conditioning Inversion）：

- **L1（Prompt Rewrite）**：通过 LLM 对 VLM caption 进行结构性改写，优化文本条件以提升 T2V 模型的生成质量
- **L2（SVD Noise Prior）**：对 DDIM 反演噪声进行 SVD 滤波，提取运动先验并注入生成过程的初始噪声
- **L3（Velocity Field Matching）**：速度场匹配，进一步约束生成轨迹（本文档暂不涉及）

理想目标是各层正交增益叠加，即 L1+L2 > max(L1, L2)。

### 1.2 L1：v7e Prompt Rewrite 策略

v7e 是当前最优的 Prompt 改写策略（commit `be25e7c`），核心设计为"4 处手术式修改 + 其余逐字复制"：

**四处修改**：

1. **OPENING**（主体前置）：将动作主体移至句首，利用 UMT5 position-0 注意力权重优势。通过 6 组正反对比示例教会 LLM 区分"动作主体"与"背景/环境"。
2. **TEMPORAL CHAIN**（时序链）：对主体运动描述的 1-2 句添加时序连接词（initially → then → gradually），增强时序连贯性。
3. **ONE VISUAL INFERENCE**（单一视觉推断）：添加一个物理隐含的材质/光照属性（如 "wooden hulls" → "dark brown hulls"），不添加新对象。
4. **CAMERA CLOSING**（镜头收束）：末尾补充镜头描述（如 "The camera remains steady in close-up"），利用 UMT5 尾部 token 权重。

**最高优先级规则 — Motion Fidelity**：
运动描述必须从输入逐字保留，禁止推断运动方向、改变运动动词、改变运动速度、添加未提及的轨迹。这确保 prompt 中的运动信息与原始视频一致。

**约束条件**：output 长度 ≈ input ±15%，不添加原文未提及的信息，5 个带 WHY 标注的 few-shot examples 引导格式。

**脚本路径**：`scripts/rewrite_hybrid.py`（SYSTEM_PROMPT 定义于 lines 64-146）

### 1.3 L2：SVD Noise Prior（运动先验注入）

SVD 滤波器对 DDIM 反演噪声进行处理，提取运动方向信息并注入生成噪声：

**三阶段流水线**：

1. **Stage 1 — Spatial Decontenting**：将噪声 reshape 为 (C×F, H×W)，SVD 分解后去除 top-k_s 空间主成分（保留 ≥10% 能量），消除空间内容信息，保留运动结构。
2. **Stage 2 — Temporal Retention**：将 Stage 1 输出 reshape 为 (C×H×W, F)，SVD 分解后保留 top-k_m 时间主成分（保留 ≥90% 能量），提取时间维度的运动方向。
3. **Stage 3 — Post-processing**：根据模式进行后处理。

**SVD 模式**：

| 模式 | 描述 | 特征 |
|------|------|------|
| v1 | 原始两阶段（spatial + temporal），不做 renorm | η_temporal std ≈ 0.28-0.41，保留低剂量方向偏置 |
| renorm | 两阶段 + 强制归一化至 std=1.0 | 噪声注入量从 ~0.019 放大至 ~0.063 |
| highfreq | 频率分离（高频细节注入）+ renorm | 理论上避免低频全局运动冲突 |
| adaptive | 根据 motion_strength 自动判断是否启用 | motion_strength < 0.15 则 skip |

**噪声混合公式**：`z_init = (1-α) × z_random + α × η_temporal`，其中 α=0.004

**脚本路径**：`src/svd_filter.py`（V2 版本，606 行，4 种模式）

**入口命令**：`scripts/run_hybrid_iter.py --noise_prior --alpha 0.004 --svd_mode <mode>`

---

## 二、实验结果总表

| # | 实验配置 | CLIP | X-CLIP | vs Baseline | vs Pure L1 | vs Pure L2 |
|---|----------|:----:|:------:|:-----------:|:----------:|:----------:|
| 0 | **Baseline**（原始 VLM caption，无 SVD） | 0.8753 | 0.7491 | — | — | — |
| 1 | **Pure L2**（原始 caption + SVD v1, α=0.004） | 0.8964 | 0.7874 | +2.4% / +5.1% | — | — |
| 2 | **Pure L1**（v7e prompt，无 SVD） | 0.8946 | 0.7727 | +2.2% / +3.2% | — | — |
| 3 | **L1+L2**（v7e + SVD v1, α=0.004） | 0.8945 | 0.7413 | +2.2% / -1.0% | **0.0% / -4.1%** | -0.2% / -5.9% |
| 4 | **L1+L2**（v7e + SVD highfreq, α=0.004） | 0.8804 | 0.7031 | +0.6% / -6.1% | **-1.6% / -9.0%** | -1.8% / -10.7% |
| 5 | **L1+L2**（v7e + SVD renorm, α=0.004） | ~0.89 | ~0.74 | — | **~0% / -4.1%** | — |
| 6 | **L1-lite+L2**（v7e-lite + SVD v1, α=0.004） | 0.8842 | 0.7788 | +1.0% / +4.0% | **-1.2% / +0.8%** | -1.4% / -1.1% |

> **注**：vs 列格式为 "CLIP变化 / X-CLIP变化"。**关键对比是 vs Pure L1 列**——L1+L2 叠加后相对 Pure L1 单独使用的增减，只有该列为正值才表明实现了 1+1>2。
>
> **核心结论**：Exp 3-5 的 vs Pure L1 列全部为负值，说明 v7e+SVD 组合相比单独使用 v7e 是**下降**的。Exp 6 (v7e-lite+SVD) X-CLIP 微升 +0.8%，但 CLIP 下降 -1.2%，未实现真正的 1+1>2。

---

## 三、各实验详细记录

### 3.1 Exp 0：Baseline（原始 VLM caption 直出）

**配置**：Qwen2.5-VL-7B 对原始视频生成 caption，不做任何后处理，直接送入 Wan2.1 生成视频。

**结果**：

| 指标 | 均值 |
|------|------|
| orig_gen_clip | **0.8753** |
| orig_gen_xclip | **0.7491** |

**定位**：作为所有实验的参照基准。VLM 原生输出语义完整，但缺少针对 T2V 模型的结构优化。

---

### 3.2 Exp 1：Pure L2（原始 caption + SVD v1）

**配置**：使用原始 VLM caption（不做 LLM 改写），仅启用 SVD v1 模式注入运动先验。

**参数**：`--noise_prior --svd_mode v1 --alpha 0.004 --skip_vlm`（跳过 VLM 重新推理，直接使用已有 caption）

**结果**：

| 指标 | 均值 | vs Baseline |
|------|------|-------------|
| orig_gen_clip | **0.8964** | +0.0211（+2.4%） |
| orig_gen_xclip | **0.7874** | +0.0383（+5.1%） |

**分析**：SVD v1 在保守的原始 caption 上取得显著正向收益。原始 caption 运动描述模糊（如 "the boat moves"），SVD 先验补充了"怎么动"的方向信息，与文本条件形成互补而非冲突。这验证了 SVD 先验的核心价值——为运动信息不足的 prompt 提供方向引导。

---

### 3.3 Exp 2：Pure L1（v7e prompt，无 SVD）

**配置**：使用 v7e LLM 改写策略优化 caption，不启用任何 SVD 噪声注入。

**参数**：不加 `--noise_prior` 标志

**生成目录**：`outputs/hybrid_v7e/generated`

**结果**：

| 指标 | 均值 | vs Baseline |
|------|------|-------------|
| orig_gen_clip | **0.8946** | +0.0193（+2.2%） |
| orig_gen_xclip | **0.7727** | +0.0236（+3.2%） |

**分析**：v7e 改写策略通过 subject-first + temporal chain + motion fidelity 三重约束，在 CLIP 和 X-CLIP 两个维度均取得稳定正向增益。特别是 Motion Fidelity 规则确保了运动描述的准确性，使得 prompt 中包含了精确的运动方向和速度信息。

---

### 3.4 Exp 3：L1+L2（v7e + SVD v1）

**配置**：v7e 改写后的 caption + SVD v1 模式注入。

**参数**：`--noise_prior --svd_mode v1 --alpha 0.004`

**结果**：

| 指标 | 均值 | vs Baseline | vs Pure L1 |
|------|------|-------------|------------|
| orig_gen_clip | **0.8945** | +2.2% | -0.01% |
| orig_gen_xclip | **0.7413** | -1.0% | **-4.1%** |

**分析**：叠加 SVD v1 后，CLIP 基本持平但 X-CLIP 相比 Pure L1 下降 4.1%。这表明 SVD 先验与 v7e 精确运动描述产生了冲突。SVD 的 direction_shift 约 2%，η_temporal std 约 0.28-0.41，虽然注入量不大（α=0.004），但当 prompt 已经精确描述了运动方向时，哪怕微小的方向偏差也会与文本条件产生矛盾信号。

**冲突机制**：v7e 通过 Motion Fidelity 规则保留了如 "drifts forward slowly" 的精确描述，T2V 模型的文本条件通道已经编码了明确的运动方向。SVD 先验通过反演噪声提取的方向可能存在微小误差（~2%），在精确 prompt 场景下，这种误差会与文本条件产生对抗而非协同。

---

### 3.5 Exp 4：L1+L2（v7e + SVD highfreq）

**配置**：v7e 改写 + SVD highfreq 模式（频率分离 + renorm）。理论假设是高频成分只包含运动细节而不含全局方向，可以避免与 prompt 的方向冲突。

**参数**：`--noise_prior --svd_mode highfreq --alpha 0.004`（ρ_s=0.1, ρ_m=0.9）

**结果**：

| 指标 | 均值 | vs Baseline | vs Pure L1 |
|------|------|-------------|------------|
| orig_gen_clip | **0.8804** | +0.6% | **-1.6%** |
| orig_gen_xclip | **0.7031** | -6.1% | **-9.0%** |

**分析**：highfreq 模式表现最差。日志分析显示滤波后帧间余弦相似度接近随机（~0.05），说明高频分量不携带连贯运动方向，注入后等效于向初始噪声中添加了高斯噪声。10 个样本中有 6 个出现 SVD 方向偏好与 prompt 指令的明显冲突。renorm 将 std 强制归一化至 1.0，放大了噪声注入量（从 ~0.019 到 ~0.063），进一步加剧了干扰。

---

### 3.6 Exp 5：L1+L2（v7e + SVD renorm）

**配置**：v7e 改写 + SVD renorm 模式（两阶段 + 强制 std=1.0）。

**参数**：`--noise_prior --svd_mode renorm --alpha 0.004`

**结果**：X-CLIP 相比 Pure L1 下降约 4.1%（具体 CLIP 数值因实验中断未完整记录）。

**分析**：renorm 破坏了时间相关性，将 η_temporal 的 std 从 ~0.3 强制拉伸到 1.0，等效于增大了噪声注入幅度约 3 倍（0.019 → 0.063）。在 v7e 的高质量 prompt 已经将文本-视频对齐推至接近天花板（CLIP ~0.895）的情况下，任何非必要的噪声扰动都会造成退化。

---

### 3.7 Exp 6：L1-lite+L2（v7e-lite + SVD v1）

**配置**：使用 v7e-lite 改写策略（3 处修改，去除 temporal chain，保留 VLM 原始运动描述不变）+ SVD v1 模式注入。

**v7e-lite 与 v7e 区别**：
- 保留：subject-first（主体前置）、one visual inference（单一视觉推断）、camera closing（镜头收束）
- 去除：temporal chain（时序链）——不再对运动描述添加时序连接词
- 核心变化：运动描述完全保留 VLM 原文，不做任何改动（verbatim copy）

**设计意图**：v7e-lite 故意让运动描述保持模糊/保守（与 VLM 原始输出一致），为 SVD 先验留出"运动信息补充"的空间，同时通过 subject-first 和 visual inference 保持外观对齐能力。

**参数**：`--noise_prior --svd_mode v1 --alpha 0.004`

**脚本路径**：`scripts/rewrite_hybrid_lite.py`

**结果**：

| 指标 | 均值 | vs Baseline | vs Pure L1 | vs Pure L2 |
|------|------|-------------|------------|------------|
| orig_gen_clip | **0.8842** | +1.0% | -1.2% | -1.4% |
| orig_gen_xclip | **0.7788** | +4.0% | +0.8% | -1.1% |

**分析**：v7e-lite + SVD v1 相比其他 L1+L2 组合有明显改善——X-CLIP 0.7788 显著优于 v7e+SVD 的 0.7413，且相比 Pure L1 的 X-CLIP (0.7727) 微升 +0.8%。但 CLIP 下降 1.2%（0.8946→0.8842），说明去掉 temporal chain 和减弱改写力度导致外观对齐能力下降。

**结论**：策略方向正确（保留运动描述模糊性给 SVD 留空间），但效果不够——X-CLIP 仅微升 0.8%，CLIP 反降 1.2%，综合来看未实现 1+1>2 的目标。问题在于 v7e-lite 牺牲了 L1 自身的外观对齐能力，换来的 SVD 增益不足以弥补。

---

## 四、核心发现

### 4.1 L1 和 L2 独立均有效，但叠加产生冲突

| 对比 | CLIP 增益 | X-CLIP 增益 |
|------|-----------|-------------|
| Pure L2 vs Baseline | +2.4% | +5.1% |
| Pure L1 vs Baseline | +2.2% | +3.2% |
| L1+L2 vs Baseline | +2.2% | -1.0% |

L1 和 L2 单独使用时都能显著超越 baseline，但组合后 X-CLIP 反而低于 baseline。这不是简单的"1+1<2"，而是"1+1<1"——叠加后比单独使用 L1 还差。

### 4.2 冲突根因：信息冗余 + 方向误差

**SVD 有效的前提**是 prompt 的运动信息不足：

- 原始 VLM caption 运动描述保守模糊 → SVD 提供互补信息 → 正向增益（+5.1% X-CLIP）
- v7e prompt 运动描述精确详尽 → SVD 提供冗余/冲突信息 → 负向干扰（-4.1% X-CLIP）

本质上，v7e 的 Motion Fidelity 规则使得 prompt 已经承担了"告诉模型怎么动"的全部职责，SVD 先验试图再次注入运动方向时，即使方向大体一致（~98% cosine similarity），剩余的 2% 误差也会在扩散模型的 ODE 求解过程中被放大，导致生成轨迹偏移。

### 4.3 SVD 模式对比

| SVD 模式 | X-CLIP vs Pure L1 | 问题 |
|----------|:------------------:|------|
| v1（原始） | -4.1% | 低频全局方向与精确 prompt 微冲突 |
| highfreq | -9.0% | 高频无连贯方向，等效随机噪声 |
| renorm | -4.1% | std 放大 3x，破坏时间相关性 |
| adaptive | -1.5% | 大部分样本被 skip，少量启用的样本仍有干扰 |

所有模式在 v7e 场景下均为负向，说明问题不在 SVD 的具体实现方式，而在于"精确 prompt + SVD 先验"这一组合的结构性矛盾。

---

## 五、历史参考数据

以下为早期实验中 L1+L2 取得正向增益的记录（使用 V4 caption 策略）：

| 配置 | CLIP | X-CLIP | 说明 |
|------|------|--------|------|
| Baseline | 0.8753 | 0.7491 | 原始 caption |
| V4 L1 only | 0.8794 | 0.7594 | V4 策略（3处修改，较保守） |
| V4 L1+L2（SVD v1） | 0.8818 | 0.7863 | L2 在 V4 上正向 +3.5% X-CLIP |
| Old caption + L2 | 0.8958 | — | 保守 prompt 上 SVD 增益最大 |
| V4 + L2 | — | 0.7863 | 最高 X-CLIP |

这进一步验证：**SVD 在运动描述越保守的 prompt 上增益越大**。V4 策略仅做 3 处结构修改，运动描述基本照抄原文（较模糊），给 SVD 留出了"运动信息补充"的空间。

---

## 六、代码与路径索引

| 文件/路径 | 说明 |
|-----------|------|
| `scripts/run_hybrid_iter.py` | 流水线入口，支持 `--noise_prior`、`--alpha`、`--svd_mode`、`--skip_vlm` |
| `scripts/rewrite_hybrid.py` | v7e Prompt 改写脚本（SYSTEM_PROMPT lines 64-146） |
| `src/svd_filter.py` | SVD 滤波器 V2（4 种模式，606 行） |
| `outputs/hybrid_v7e/generated/` | Pure L1 生成结果 |
| `outputs/hybrid_v7e/captions_final/` | v7e 改写后的 caption |
| `outputs/hybrid_v7e_svd_v1/` | L1+L2 (v1) 生成结果 |
| `outputs/hybrid_v7e_svd_highfreq/` | L1+L2 (highfreq) 生成结果 |

---

## 七、VLM Caption 特征分析（200 样本）

### 7.1 基础统计

对 200 个 VLM 原始 caption（Qwen2.5-VL-7B 生成）进行了系统分析：

| 指标 | 值 |
|------|-----|
| 总文件数 | 200 |
| 平均词数 | 109 词 |
| 中位数 | 105 词 |
| 范围 | 73 ~ 260 词 |
| 单段落（1-2行） | 172 个 (86%) |
| 多段落（3+行） | 28 个 (14%) |

### 7.2 结构性问题

**问题 1：开头浪费 position-0**

100% 的 caption 以 "The video depicts/features/captures/shows..." 开头：

| 开头模板 | 数量 |
|----------|------|
| "The video depicts" | 99 |
| "The video features" | 45 |
| "The video captures" | 36 |
| "The video shows" | 19 |

UMT5 对 position-0 附近的 token 赋予最高注意力权重，但这些 caption 把最重要的位置给了无用的 "The video" 而非实际主体。

**问题 2：尾部被情感总结占据**

144/200 (72%) 以 "The overall atmosphere/mood/scene..." 结尾。这些抽象情感词（"serene", "whimsical", "evoking a sense of"）对 T2V 模型的具体视觉生成没有指导意义，浪费了 UMT5 尾部 token 的权重位置。

典型尾句："The overall atmosphere is serene and whimsical, blending elements of a typical coastal town with the natural beauty of an underwater world."

**问题 3：大量猜测/对冲词**

159/200 (80%) 包含 "suggesting", "indicating", "appears to be", "might be", "possibly", "likely" 等对冲词。全部 200 个 caption 中共 175 处此类表达（平均每个 0.87 处）。

这些词让 text encoder 的编码变得模糊——attention 分散在不确定性修饰词上，而非实际视觉内容。

**问题 4：缺少镜头语言**

只有 40/200 (20%) 提到 camera 相关描述。80% 的 caption 没有任何镜头/视角信息，T2V 模型只能随机选择镜头方式。

**问题 5：运动描述保守模糊**

124/200 (62%) 包含运动动词，但描述通常很泛化："moving forward", "running energetically", "driving on a road"。没有精确的方向（left/right）、速度（slowly/rapidly with exact pacing）或轨迹描述。**这恰好是 SVD 先验的优势空间——prompt 没说清楚的运动细节，SVD 来补充。**

### 7.3 微调策略设计：v8-minimal

**核心原则**：在 Pure L2 基础上，只做不碰运动的结构优化，让 SVD 继续独占运动通道。

**流程**：VLM 原始 caption → LLM 一次微调 → 直出（无需 VLM 验证，因为不做内容推断）

**三处改动**：

| # | 改动 | 做什么 | 为什么不和 SVD 冲突 |
|---|------|--------|-------------------|
| 1 | 主体前置 | 去掉 "The video depicts..."，直接以主体名词开头 | 只调语序，不改内容 |
| 2 | 去猜测 + 去尾部总结 | 删除 "suggesting/possibly/might be" 和 "The overall atmosphere..." 句 | 删掉的是噪声信息 |
| 3 | 补镜头语 | 末尾加一句具体的镜头描述（steady/tracking/close-up/wide shot） | 镜头是全局信息，不影响物体运动方向 |

**不做的事**（红线）：
- 不改任何动词（原文 "driving" 不改成 "speeding"）
- 不加方向/速度（原文没说往哪走就不加）
- 不加时序词（不加 "initially → then → gradually"）
- 不做视觉推断（不加原文没提到的颜色/材质）
- 不加新运动

**示例**：

原始：
> The video depicts a white SUV driving on a dusty, unpaved road through a forested area. The vehicle is equipped with roof racks carrying luggage or gear, suggesting it might be on a journey or adventure. As the SUV moves forward, it kicks up a cloud of dust behind it, indicating the dryness of the terrain and the speed at which it is traveling. The surrounding environment features tall pine trees and a scenic view of distant mountains under a clear blue sky. The overall atmosphere conveys a sense of exploration and outdoor adventure.

微调后：
> A white SUV drives on a dusty, unpaved road through a forested area. The vehicle is equipped with roof racks carrying luggage. As the SUV moves forward, it kicks up a cloud of dust behind it. The surrounding environment features tall pine trees and a scenic view of distant mountains under a clear blue sky. The camera follows from behind in a steady tracking shot.

改动点：(1) 主体前置 "A white SUV" 到句首；(2) 删除 "suggesting it might be on a journey or adventure"、"indicating the dryness..."、"The overall atmosphere..." 三处废话；(3) 补镜头语 "The camera follows from behind in a steady tracking shot."

**预期效果**：CLIP 从 0.8964 微升（主体前置让 UMT5 更好编码），X-CLIP 维持 0.7874（运动完全由 SVD 主导，prompt 未干扰）。

**脚本路径**：`scripts/rewrite_minimal.py`

---

## 八、结论与下一步方向

当前实验明确揭示了一个核心矛盾：**v7e 的 Motion Fidelity 设计使 prompt 精确到足以独占运动信息通道，SVD 先验因信息冗余反而成为干扰源**。

转换思路后的新策略：**不再试图让 L1 和 L2 各管运动，而是让 L1 只做 prompt 结构优化（不碰运动），L2 (SVD) 完全负责运动方向。**具体方案为 v8-minimal：在 Pure L2 (CLIP 0.8964, X-CLIP 0.7874) 的基础上，通过最小化的 prompt 结构微调（主体前置、去废话、补镜头），在不干扰 SVD 运动通道的前提下进一步提升 CLIP。

---

## 九、后续实验计划

**Exp 7：v8-minimal + SVD v1**

- 配置：v8-minimal 改写 + SVD v1 (α=0.004)
- 预期：CLIP ≥ 0.8964, X-CLIP ≥ 0.7874（两者均不低于 Pure L2）
- 脚本：`scripts/rewrite_minimal.py` + `run.py --noise_prior --alpha 0.004 --svd_mode v1`
- 意义：如果成功，则实现了 L1+L2 > L2（prompt 微调在 SVD 基础上带来额外增益），论文可以讲 "hierarchical conditioning where each layer contributes non-overlapping information"
